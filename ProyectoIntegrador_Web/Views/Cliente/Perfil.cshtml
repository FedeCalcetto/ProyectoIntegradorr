@model ProyectoIntegrador_Web.Models.EditarClienteViewModel

@{
    ViewData["Title"] = "Perfil";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-sm border-0 rounded-3">
                <div class="card-body p-4">
                    <h2 class="text-center mb-3 text-primary fw-semibold">Mi Perfil</h2>
                    <p class="text-center text-muted mb-4">Actualiza tus datos personales</p>

                    @if (TempData["Mensaje"] != null)
                    {
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            @TempData["Mensaje"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }

                    <form asp-action="Perfil" method="post">
                        @Html.AntiForgeryToken()
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="mb-3">
                            <label asp-for="Nombre" class="form-label fw-semibold"></label>
                            <input asp-for="Nombre" class="form-control" placeholder="Tu nombre" />
                            <span asp-validation-for="Nombre" class="text-danger small"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Apellido" class="form-label fw-semibold"></label>
                            <input asp-for="Apellido" class="form-control" placeholder="Tu apellido" />
                            <span asp-validation-for="Apellido" class="text-danger small"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Email" class="form-label fw-semibold"></label>
                            <input asp-for="Email" class="form-control bg-light" readonly />
                        </div>

                        <div class="mb-3">
                            <label asp-for="Password" class="form-label fw-semibold"></label>
                            <input asp-for="Password" type="password" class="form-control" placeholder="Nueva contraseña" />
                            <span asp-validation-for="Password" class="text-danger small"></span>
                        </div>

                        <h5 class="mt-4 text-secondary fw-semibold border-bottom pb-1">Dirección</h5>

                        <div class="mb-3 mt-2">
                            <label asp-for="Domicilio" class="form-label fw-semibold"></label>
                            <input asp-for="Domicilio" class="form-control" placeholder="Calle y número" />
                        </div>

                        <div class="mb-3">
                            <label asp-for="Departamento" class="form-label fw-semibold"></label>
                            <input asp-for="Departamento" class="form-control" placeholder="Departamento / Piso" />
                        </div>

                        <div class="mb-4">
                            <label asp-for="Barrio" class="form-label fw-semibold"></label>
                            <input asp-for="Barrio" class="form-control" placeholder="Barrio o zona" />
                        </div>

                        <div class="d-flex justify-content-between">
                            <a asp-action="Inicio" class="btn btn-outline-secondary">Volver</a>
                            <button type="submit" class="btn btn-primary">Guardar cambios</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>


